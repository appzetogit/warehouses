# npm 10 rejects @mui/x-date-pickers' optional date-fns-jalali peer range against
# the exact prerelease react-day-picker pins; npm 11 accepts it. Versions still
# come from package-lock.json.
legacy-peer-deps=true
